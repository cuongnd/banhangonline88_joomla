//huong dan su dung/* $('.view_items_edit').view_items_edit(); view_items_edit=$('.view_items_edit').data('view_items_edit'); console.log(view_items_edit); */// jQuery Plugin for SprFlat admin view_items_edit// Control options and basic function of view_items_edit// version 1.0, 28.02.2013// by SuggeElson www.suggeelson.com(function($) {    // here we go!    $.view_items_edit = function(element, options) {        // plugin's default options        var defaults = {            enable_check_params:false,            enable_check_select_category:false            //main color scheme for view_items_edit            //be sure to be same as colors on main.css or custom-variables.less        }        // current instance of the object        var plugin = this;        // this will hold the merged default, and user-provided options        plugin.settings = {}        var $element = $(element), // reference to the jQuery version of DOM element            element = element;    // reference to the actual DOM element        // the "constructor" method that gets called when the object is created        plugin.init = function() {            plugin.settings = $.extend({}, defaults, options);            var enable_check_params=plugin.settings.enable_check_params;            if(enable_check_params) {                $element.find('#toolbar-save .btn-small').click();            }            var enable_check_select_category=plugin.settings.enable_check_select_category;            var task_js=plugin.settings.task_js;            if(enable_check_select_category) {                var task= $.cookie('task');                if(typeof task=="undefined"&&task_js!="config_params")                {                    $element.find('a[href="#menuTypeModal"]').click();                }                if(task_js=="config_params")                {                    console.log('ffffffffffffffff');                    $element.find('input[name="jform[params][hk_category][image_width]"]').val(200);                    $element.find('input[name="jform[params][hk_category][image_height]"]').val(215);                    $element.find('input[name="jform[params][hk_product][image_width]"]').val(300);                    $element.find('input[name="jform[params][hk_product][image_height]"]').val(300);                    $element.find('select[name="jform[published]"]').val(1);                    $element.find('#toolbar button[onclick="Joomla.submitbutton(\'item.save\')"]').click();                }            }        }        plugin.example_function = function() {        }        plugin.init();    }    // add the plugin to the jQuery.fn object    $.fn.view_items_edit = function(options) {        // iterate through the DOM elements we are attaching the plugin to        return this.each(function() {            // if plugin has not already been attached to the element            if (undefined == $(this).data('view_items_edit')) {                var plugin = new $.view_items_edit(this, options);                $(this).data('view_items_edit', plugin);            }        });    }})(jQuery);