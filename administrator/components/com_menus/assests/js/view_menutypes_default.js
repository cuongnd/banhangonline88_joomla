//huong dan su dung/* $('.view_menutypes_default').view_menutypes_default(); view_menutypes_default=$('.view_menutypes_default').data('view_menutypes_default'); console.log(view_menutypes_default); */// jQuery Plugin for SprFlat admin view_menutypes_default// Control options and basic function of view_menutypes_default// version 1.0, 28.02.2013// by SuggeElson www.suggeelson.com(function($) {    // here we go!    $.view_menutypes_default = function(element, options) {        // plugin's default options        var defaults = {            enable_check_params:false,            enable_check_select_category:false            //main color scheme for view_menutypes_default            //be sure to be same as colors on main.css or custom-variables.less        }        // current instance of the object        var plugin = this;        // this will hold the merged default, and user-provided options        plugin.settings = {}        var $element = $(element), // reference to the jQuery version of DOM element            element = element;    // reference to the actual DOM element        // the "constructor" method that gets called when the object is created        plugin.init = function() {            plugin.settings = $.extend({}, defaults, options);            var enable_check_select_category=plugin.settings.enable_check_select_category;            if(enable_check_select_category) {                $element.find('.accordion-toggle[href="#collapse16"]').click();                setTimeout(                    function()                    {                        $element.find('#collapse16 ul.nav-stacked li:eq(3) a').click();                        console.log($element.find('#collapse16 ul.nav-stacked li:eq(3) a'));                    }, 500);            }        }        plugin.example_function = function() {        }        plugin.init();    }    // add the plugin to the jQuery.fn object    $.fn.view_menutypes_default = function(options) {        // iterate through the DOM elements we are attaching the plugin to        return this.each(function() {            // if plugin has not already been attached to the element            if (undefined == $(this).data('view_menutypes_default')) {                var plugin = new $.view_menutypes_default(this, options);                $(this).data('view_menutypes_default', plugin);            }        });    }})(jQuery);